<h1>タスク一覧</h1>

<% @tasks.each do |task| %>
    <ul>
        <li>
            id:<%= link_to task.id, task %> 状態：<%= task.status %> ：内容:<%= task.content %>
        </li>
    </ul>
<% end %>

<%= link_to '新規登録', new_task_path %>

